# LLM Proxy Configuration
# This file configures rate limiting, cost tracking, and model-specific settings

# Development configuration

enabled: true

features:
  cost_tracking:
    enabled: false
  api_key_management:
    enabled: false
  rate_limiting:
    enabled: false

# Enable local LLM providers for development
local_llms:
  gpt-oss:
    enabled: true
    default_model: "openai/gpt-oss-120b"
    thinking_tag_fix: true
    request_timeout: 120
    max_retries: 2
    models:
      "openai/gpt-oss-120b":
        enabled: true
        endpoints:
          - url: "http://dgx01.arcs.oregonstate.edu:8000/v1"
            api_key: ""
        pricing:
          input: 0.1
          output: 0.4
  qwen:
    enabled: true  # Enable for development testing
    default_model: "qwen/qwen3-next-80b-a3b-thinking"
    thinking_tag_fix: true
    request_timeout: 120
    max_retries: 2
    models:
      "qwen/qwen3-next-80b-a3b-thinking":
        enabled: true
        endpoints:
          - url: "http://dgx01.arcs.oregonstate.edu:8005/v1"
            api_key: ""
        pricing:
          input: 0.0
          output: 0.0

# Enable Claude Code proxy for development
claude_code_proxy:
  enabled: true
  target_provider: "qwen"
  target_model: "qwen/qwen3-next-80b-a3b-thinking"
  unified_endpoint: "/cc-local/v1/messages"
  supported_providers: ["qwen", "gpt-oss"]
  parameter_mapping:
    max_tokens: "max_tokens"


